test = reshape(mapstd(reshape(double(imread('../cyfry_test/test1.bmp')),1,[])),480,640);

[out,cnet] = sim(cnet, test);

t_out = ones(120,160) * min(min(out));
t_out(5:116,5:157) = out;
pos = subsample(t_out,4,4,0);

subplot(